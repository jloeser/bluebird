{
  "Name": "Virtual Machines",
  "Members": [
    {% for domain in libvirt.get_domains() %}
      {
        "href": "/redfish/v1/Systems/{{ domain.name() }}"
      }
    {% if not loop.last %},{% endif %}
    {% endfor %}
    ],
  "Oem": {
    "Total": {{ total }},
    "Title": "ComputerSystemCollection",
    "Schema": "http://redfish.dmtf.org/schemas/v1/ComputerSystemCollection.json"
  }
}

